{
  "name": "nate-protocol",
  "version": "1.0.0",
  "description": "Decentralized Human Execution Protocol - NAV-Backed Growth Token",
  "main": "index.js",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "test:gas": "REPORT_GAS=true npx hardhat test",
    "node": "npx hardhat node",
    "deploy:local": "hardhat run scripts/deploy_system.js --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy_system.js --network sepolia",
    "deploy:mainnet": "npx hardhat run scripts/deploy.js --network mainnet",
    "verify": "npx hardhat verify --network sepolia",
    "clean": "npx hardhat clean"
  },
  "keywords": [
    "ethereum",
    "solidity",
    "defi",
    "human-capital",
    "token"
  ],
  "author": "Nate",
  "license": "MIT",
  "devDependencies": {
    "@chainlink/functions-toolkit": "^0.3.2",
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@chainlink/contracts": "^1.5.0",
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1"
  }
}
